# 2026-02-24

- [06:25] 使用者偏好：對於「定期執行的任務」，不再在每次觸發時詢問；改為自動執行。
  - 預設行為：自動執行排程腳本（例如 update_promotions.py）。
  - 通知偏好：預設僅在發生錯誤或需要人工介入時通知。若使用者希望每次都收到執行成功/失敗通知，請另行指示。
- 記錄者：OpenClaw 助手（session）

- [07:00] 已建立每日維護（maintenance）流程：
  - 腳本：workspace/maintenance/daily_maintenance.py（會執行 fetch、update_promotions.py、update_html.py、py_compile、autopep8(若可用) 並在本地 commit auto-fixes；不會自動推到遠端）
  - 執行時間：每天 03:00（Asia/Taipei）由 cron 執行 /home/joe/.openclaw/workspace/maintenance/run_daily.sh
  - 建議：預設為「不自動 push、不自動 deploy、只在錯誤時通知、每天搜尋外部並產生建議清單（但不自動套用）」。
  - 建議的後續授權：如果允許自動 push 或建 PR，請回覆授權類型（自動推 main / 建分支+PR / 不自動推）。
