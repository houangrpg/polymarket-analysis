{
  "name": "my-website",
  "version": "1.0.0",
  "description": "Fix for missing website files",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building website assets...' && mkdir -p dist && cp -r dashboard/* dist/",
    "deploy": "echo 'Deploying website...' && cp -r dist/* /var/www/html/"
  },
  "author": "Joe Huang",
  "license": "ISC"
}